<table class="table">
  <% @photos.each do |photo| %>
      <tr>
        <td width="20%"><%= link_to(image_tag(photo.photo_image.url(:thumb), class: 'thumbnail'), photo) %></td>
        <td width="80%"><%= photo.title %></td>
      </tr>
  <% end %>
</table>
