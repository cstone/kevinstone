<%= semantic_form_for @photo, html: {multipart: true, class: "form-horizontal"} do |f| %>

    <table class="table">
      <tr>
        <td>Title:</td>
        <td><%= f.input :title, label: false %></td>
      </tr>
      <tr>
        <td>Description:</td>
        <td><%= f.input :description, label: false %></td>
      </tr>
      <tr>
        <td>Photo:</td>
        <td><%= f.file_field :photo_image, label:false %></td>
      </tr>
      <tr>
        <td colspan="2"><%= f.action :submit, :as => :input, class: 'btn btn-primary' %></td>
      </tr>
    </table>
<% end %>
