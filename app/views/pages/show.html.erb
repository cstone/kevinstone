<ol class="breadcrumb">
  <li><%= link_to 'Home', root_path %></li>
  <li class="active"><%= @page.name %></li>
</ol>
<h3><%= @page.name %></h3>
<% if @page.page_image.url %>
<%= image_tag @page.page_image.url, class: 'img-float-right img-responsive' %>
<% end %>
<%= simple_format @page.content %>
